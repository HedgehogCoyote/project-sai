# 2026-08-24 SAI 개발 기록

## 직접 진행한 작업

- 세션 사용자 ID를 `SessionConst.LOGIN_USER_ID` 상수로 관리하도록 정리했다.
- 비로그인 상태에서 `GET /api/auth/me`를 호출하면 401 응답을 반환하도록 예외와 공통 처리기를 추가했다.
- 회원가입, 로그인, 로그아웃, 현재 사용자 조회를 검증할 `AuthControllerTest`의 구조와 일부 테스트를 직접 작성했다.

## AI에게 요청한 작업

- `MockMvc`, Mockito의 `given().willReturn()`과 응답 검증 방법을 질문하고 설명을 받았다.
- 비어 있던 회원가입·로그인 테스트를 포함한 `AuthControllerTest` 완성본 작성과 코드 검토를 요청했다.

## 다음 작업

- 완성된 테스트 코드를 프로젝트에 반영하고 실제 컴파일 및 테스트 통과 여부를 확인한다.
- 이후 잘못된 아이디·비밀번호와 중복 회원가입 로직을 검증하는 `AuthServiceTest`를 작성한다.
