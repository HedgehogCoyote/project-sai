# 2026-08-26 SAI 개발 기록

## 직접 진행한 작업

- `Space`, `SpaceMember` 엔티티와 `SpaceMemberRole` 역할 enum을 작성했다.
- `SpaceRepository`, `SpaceMemberRepository`를 작성하고 사용자와 공간 관계 조회 메서드를 추가했다.
- 공간 생성 요청·응답 DTO와 삭제 요청·응답 DTO 초안을 작성했다.
- `space_member` 테이블에 역할 컬럼을 추가하기 위한 Flyway V2 마이그레이션을 작성했다.

## AI에게 요청한 작업

- 로컬 `project-sai`의 현재 파일 구조와 백엔드 코드를 실제 체크아웃 기준으로 점검했다.
- `SpaceRepository`는 현재 기본 JPA 기능만으로 충분하며, 다음 단계가 공간 생성 Service 구현임을 확인했다.
- 컴파일 검사에서 `SpaceController`의 `RestController`, `RequestMapping` import 누락을 확인했다.
- 공간 제목 길이가 DTO 30자, 엔티티 50자, DB 100자로 서로 다른 점과 Flyway 마이그레이션 주의사항을 확인했다.

## 현재 확인된 상태

- Space 패키지와 V2 마이그레이션은 아직 Git에 추가되지 않은 작업 중 코드다.
- `SpaceService`와 실제 Space API 메서드는 아직 구현되지 않았다.
- 현재 백엔드는 `SpaceController`의 import 누락으로 컴파일되지 않는다.

## 다음 작업

- `SpaceController`의 누락된 import를 추가하고 컴파일을 다시 확인한다.
- 공간 제목 길이 기준을 하나로 정리한다.
- 하나의 트랜잭션에서 공간과 `OWNER` 구성원을 함께 저장하는 `SpaceService#createSpace`를 구현한다.
- `POST /api/space`와 세션 인증을 연결한다.
- Space Service 단위 테스트와 Controller 슬라이스 테스트를 작성한다.
